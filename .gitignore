# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini

# Local archives / manual backups (not source)
files.zip
Backup/

# Claude Code local + temp files (never commit any .claude/, at any depth)
.claude/

# build tooling
node_modules/
